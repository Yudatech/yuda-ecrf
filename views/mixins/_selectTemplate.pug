mixin _selectTemplate(name, text, value, options)
  .form-group
    label.col-sm-4.control-label #{text}
    .col-sm-8
      select.form-control(name=name)
        each optionItem in options
          option(value=optionItem.value selected=(value === optionItem.value)) #{optionItem.text}
